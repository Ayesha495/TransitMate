# Frontend (React + Vite)

This folder contains a minimal Vite + React scaffold that consumes the Django API.

Quick start:

```powershell
cd frontend
npm install
npm run dev
```

The React app expects the Django API to be running at `http://127.0.0.1:8000`.
